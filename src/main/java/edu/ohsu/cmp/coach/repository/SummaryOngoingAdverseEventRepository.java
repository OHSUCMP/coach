package edu.ohsu.cmp.coach.repository;

import edu.ohsu.cmp.coach.entity.SummaryOngoingAdverseEvent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SummaryOngoingAdverseEventRepository extends JpaRepository<SummaryOngoingAdverseEvent, Long> {
}
